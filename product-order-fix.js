(()=>{const root=document.getElementById('modalContent');if(!root)return;const tagSections=()=>{root.querySelectorAll('.size-block').forEach(block=>{const label=(block.querySelector('.size-label .eyebrow')?.textContent||'').trim().toUpperCase();block.classList.toggle('color-section',label==='SELECT COLOR');block.classList.toggle('size-section',label==='SELECT SIZE');});};new MutationObserver(tagSections).observe(root,{childList:true,subtree:true});tagSections();})();